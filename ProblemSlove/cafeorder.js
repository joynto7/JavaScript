// Sample Input (as variable values)
let coffeePrice = 5; // [cite: 10]
let pastryPrice = 3.5; // [cite: 11]
let coffeeQuantity = 2; // [cite: 12]
let pastryQuantity = 1; // [cite: 13]

// Calculate the total cost
let totalCost = (coffeePrice * coffeeQuantity) + (pastryPrice * pastryQuantity);

// Display the total cost
console.log(`The total cost of your order is $${totalCost}.`); // [cite: 15]

// Output for the sample: The total cost of your order is $13.5.